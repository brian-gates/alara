#!/bin/bash
ROOT_PATH=$( cd "$(dirname "${BASH_SOURCE[0]}")/../" ; pwd -P )
$ROOT_PATH/node_modules/tiddlywiki/tiddlywiki.js $ROOT_PATH/wiki/ --server 8080 $:/core/save/all text/plain text/html "" "" 0.0.0.0
